import CreateNeedWizard from "@/components/CreateNeedWizard";

export default function CreatePage() {
  return (
    <main className="min-h-screen bg-gradient-to-b from-yellow-50 via-sky-50 to-orange-50">
      <CreateNeedWizard />
    </main>
  );
}